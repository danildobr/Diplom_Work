
---

### 🧾 Документация: Django Backend-приложение автоматизации закупок

#### 1. **Описание приложения**

Это **бэкенд-приложение**, разработанное на **Django** и **Django REST Framework (DRF)**, для **автоматизации процесса закупок** в розничной сети. Приложение предоставляет **REST API**, через который взаимодействуют:

- **Клиенты (покупатели)**: делают заказы, управляют корзиной, просматривают заказы.
- **Поставщики**: управляют своим ассортиментом, получают заказы, включают/выключают приём заказов.
- **Администраторы** (опционально): управляют статусами заказов.

Приложение поддерживает **аутентификацию по токену**, **управление заказами**, **работу с корзиной**, **импорт/экспорт товаров**, **отправку email-уведомлений**  и **масштабируемую архитектуру** с использованием Docker.

---

#### 2. **Функционал**

- **Аутентификация и авторизация**:
    - Регистрация/логин пользователей (client/supplier).
    - Аутентификация через токен (TokenAuthentication).
- **Управление товарами**:
    - Получение списка товаров и категорий (публично).
    - Добавление/обновление товаров поставщиком через API.
- **Управление корзиной**:
    - Добавление/удаление/обновление количества товаров.
- **Управление заказами**:
    - Оформление заказа (с подтверждением по email коду).
    - Получение списка заказов (своих).
    - (Админка) Изменение статуса заказа.
- **Управление адресами доставки**:
    - Добавление/удаление/получение адресов (своих).
- **Функционал поставщика**:
    - Получение списка заказов, содержащих его товары.
    - Включение/отключение приёма заказов.
- **Отправка email**:
    - Подтверждение заказа клиенту (с кодом).

---

#### 3. **Установка**

##### 3.1. **Требования**

- Python 3.10+
- PostgreSQL
- Docker и Docker Compose (опционально, для запуска в контейнерах)
- pip (для установки зависимостей)

##### 3.2. **Шаги установки**

1.  **Клонируйте репозиторий** (или скачайте архив):
    ```bash
    git clone https://github.com/danildobr/Diplom_Work
    cd My_first_store
    ```

2.  **Создайте и активируйте виртуальное окружение**:
    ```bash
    python -m venv .venv
    # Windows:
    .venv\Scripts\activate
    # Linux/Mac:
    source .venv/bin/activate
    ```

3.  **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте файл `.env`**:
    - Создайте файл `.env` в корне проекта.
    - Укажите **настройки базы данных** и **настройки email** (если планируете использовать):
      ```
      # Настройки базы данных (локально)
      DB_NAME=procurement_db
      DB_USER=procurement_user
      DB_PASSWORD=ваш_пароль
      DB_HOST=localhost
      DB_PORT=5432

      # Настройки email (если используются)
      EMAIL_HOST=smtp.mail.ru
      EMAIL_PORT=587
      EMAIL_USE_TLS=true
      EMAIL_HOST_USER=ваш_email@mail.ru
      EMAIL_HOST_PASSWORD=ваш_пароль_приложения
      DEFAULT_FROM_EMAIL=ваш_email@mail.ru
      ```

5.  **Настройте Docker** (если используете Docker Compose):
    - Убедитесь, что Docker Desktop запущен.
    - Убедитесь, что в `docker-compose.yml` указаны правильные **переменные окружения** для подключения к БД **внутри контейнера**.
    - Убедитесь, что в `.env` (в той же папке, что и `docker-compose.yml`) указаны правильные **переменные окружения** для **контейнера БД**:
      ```
      POSTGRES_DB=my_first_store
      POSTGRES_USER=имя
      POSTGRES_PASSWORD=пароль
      ```
    - **Соберите и запустите контейнеры**:
      ```bash
      docker-compose up -d
      ```
    - **Примените миграции** (см. раздел 4.2).

6.  **Примените миграции** (если **не** используете Docker Compose или **уже запустили** его и хотите выполнить миграции **вручную**):
    ```bash
    python manage.py migrate
    ```

7.  **(Опционально) Создайте суперпользователя**:
    ```bash
    python manage.py createsuperuser
    ```

---

#### 4. **Использование**

##### 4.1. **Запуск локально (без Docker)**

1.  Убедитесь, что виртуальное окружение активировано.
2.  Убедитесь, что PostgreSQL запущен и `settings.py` (или `.env`) настроены на **локальное подключение**.
3.  Убедитесь, что миграции применены.
4.  Запустите сервер:
    ```bash
    python manage.py runserver
    ```
    Приложение будет доступно по адресу `http://127.0.0.1:8000`.

##### 4.2. **Запуск в Docker**

1.  Убедитесь, что Docker Desktop запущен.
2.  Убедитесь, что `docker-compose.yml` и `.env` настроены.
3.  Выполните миграции **внутри контейнера** (см. раздел "Как сделать в докер компосе автоматическую миграцию" выше, или выполните вручную):
    ```bash
    docker exec -it procurement_app bash
    python manage.py migrate
    ```
4.  Приложение будет доступно по адресу `http://127.0.0.1:8000`.

##### 4.3. **Тестирование API**

- Используйте файл `requests.http` (или `requests.rest`) для отправки API-запросов.
- Примеры запросов:
    - `POST /api/auth/register/` — регистрация.
    - `POST /api/auth/login/` — логин.
    - `GET /api/products/` — получить товары.
    - `POST /api/basket/add/` — добавить в корзину.
    - `POST /api/orders/create/` — оформить заказ.
    - и т.д.

---

#### 5. **Структура проекта (после разделения)**

```
My_first_store/
├── data_json.py          # Файл с данными для импорта
├── docker-compose.yml    # Конфигурация Docker Compose
├── Dockerfile            # Инструкции для сборки образа Docker
├── manage.py             # Django CLI
├── requirements.txt      # Зависимости Python
├── requests.http         # Примеры API-запросов
├── .env                  # Файл с переменными окружения
├── .gitignore
├── shema_db.md
├── My_first_store/       # Настройки Django
│   ├── __init__.py
│   ├── settings.py       # Основные настройки
│   ├── urls.py           # Главный URLconf
│   └── wsgi.py
├── core/                 # Модели и сериализаторы
│   ├── __init__.py
│   ├── models.py
│   ├── serializers.py
│   ├── urls.py           
│   ├── views.py          
│   └── management/
│       └── commands/
│           └── import_products.py
├── users/                # Аутентификация, профиль
│   ├── __init__.py
│   ├── urls.py
│   └── views.py
│
├── basket/               # Корзина
│   ├── __init__.py
│   ├── urls.py
│   └── views.py
├── orders/               # Заказы
│   ├── __init__.py
│   ├── urls.py
│   └── views.py
├── supplier_api/         # API для поставщиков
│   ├── __init__.py
│   ├── urls.py
│   └── views.py
├── templates/
    └── email/
        ├── order_confirmation_request.html
        └── password_reset_email.html
```

---

#### 6. **Дополнительно**

- **База данных**: PostgreSQL.
- **Аутентификация**: Token-based.
- **API**: RESTful.
- **Формат данных**: JSON.
- **Контейнеризация**: Docker, Docker Compose.
- **Импорт данных**: через кастомную команду `python manage.py import_products` (или через API поставщика).
- **Отправка email**: SMTP (настроено для Mail.ru).
- **Админка Django**: доступна по `/admin/` (если создан суперпользователь).

---

#### 7. **Заключение**

Это приложение предоставляет основу для системы автоматизации закупок. Его можно расширять, добавляя новые функции, улучшая логику, интегрируя с внешними системами и масштабируя.